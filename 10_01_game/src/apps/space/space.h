#pragma once
#include "libserver/thread_mgr.h"

// 初始化空间组件的函数
// 该函数通常用于注册与空间（如游戏世界、场景管理等）相关的组件
// 并且将这些组件添加到线程管理器中以便进行调度。
// 当前为空实现，未来可扩展。
inline void InitializeComponentSpace(ThreadMgr* pThreadMgr)
{

}
